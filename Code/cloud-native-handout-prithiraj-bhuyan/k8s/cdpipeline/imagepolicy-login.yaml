apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: loginimagepol
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: login
  filterTags:
    pattern: '^(?P<ts>.*)-.*'
    extract: '$ts'
  policy:
    numerical:
      order: asc