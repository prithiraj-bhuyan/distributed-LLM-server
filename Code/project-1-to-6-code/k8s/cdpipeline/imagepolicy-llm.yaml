apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: llmimagepol
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: llm
  filterTags:
    pattern: '^(?P<ts>.*)-.*'
    extract: '$ts'
  policy:
    numerical:
      order: asc